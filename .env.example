# Axiom Infrastructure — Environment Variables Reference
# Set these in Cloudflare Pages > Settings > Environment Variables
# (mark secrets as Encrypted)
#
# DO NOT commit actual values. This file is documentation only.

# ─────────────────────────────────────────────────────────────────────────────
# BOOTSTRAP
# ─────────────────────────────────────────────────────────────────────────────
# Controls whether the system will create the initial admin user on first start.
# Set to 'true' only for inital deploy, then remove/set to 'false' immediately.
# Default: false (no bootstrap)
BOOTSTRAP_ENABLED=false

# ─────────────────────────────────────────────────────────────────────────────
# CRYPTOGRAPHY
# ─────────────────────────────────────────────────────────────────────────────
# PBKDF2-SHA256 iterations for password hashing.
# Range: 50000–200000. Default: 100000.
# Higher = slower to brute force, but slower to hash (CF Worker CPU budget).
# Do not change after users have been created (existing hashes store their own iteration count).
PBKDF2_ITERS=100000
