---
import { Button } from '../ui/button';
import { Eyebrow } from '../ui/Eyebrow';

type Kind = 'contact' | 'project';

type Props = {
  kind: Kind;
  endpoint: '/api/contact' | '/api/intake';
  title: string;
  eyebrow: string;
  description: string;
  helper: string;
  submitLabel: string;
  successTitle: string;
  successText: string;
  noteLinkHref: string;
  noteLinkLabel: string;
  noteText: string;
};

const {
  kind,
  endpoint,
  title,
  eyebrow,
  description,
  helper,
  submitLabel,
  successTitle,
  successText,
  noteLinkHref,
  noteLinkLabel,
  noteText,
} = Astro.props as Props;

const fieldLabelClass = 'axiom-eyebrow text-[12px] font-medium uppercase tracking-[0.2em] text-[var(--text-muted)]';
const fieldHelpClass = 'text-[14px] leading-[1.65] text-[var(--text-secondary)] md:text-[13px]';
const fieldErrorClass = 'min-h-[1.1em] text-[14px] leading-[1.65] text-[var(--text-error)] md:text-[13px]';
const pricingPathOptions = [
  { value: 'monthly', label: 'Monthly plan' },
  { value: 'one_time', label: 'One-time ownership' },
  { value: 'not_sure', label: 'Not sure yet' },
] as const;
---

<article class="overflow-hidden rounded-[var(--radius-card)] border border-[color:var(--hairline)] bg-[var(--surface-1)] shadow-[var(--shadow-card-elevated)]">
  <div class="border-b border-[color:var(--hairline)] p-6 md:p-7 lg:p-8">
    <div class="flex flex-col gap-2 md:flex-row md:items-end md:justify-between">
      <div>
        <Eyebrow>{eyebrow}</Eyebrow>
        <h2 class="section-heading mt-2 text-[28px] font-semibold text-[var(--text-primary)]">{title}</h2>
      </div>
      <p class="section-copy">{description}</p>
    </div>
  </div>

  <div class="p-6 md:p-7 lg:p-8">
    <div data-intake-success class="hidden rounded-[var(--radius-card)] border border-[color:var(--hairline)] bg-[var(--surface-2)] p-6 shadow-[var(--shadow-card)] md:p-7" role="status" aria-live="polite">
      <div class="flex items-start gap-4">
        <div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-full border border-[color:var(--accent-border)] bg-[color:var(--accent-surface)] text-[var(--accent-solid)] shadow-[var(--shadow-brand-ring-soft)]" aria-hidden="true">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
            <path d="M20 6L9 17L4 12" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"></path>
          </svg>
        </div>
        <div class="min-w-0 text-left">
          <Eyebrow className="text-[var(--accent-solid)]">{successTitle}</Eyebrow>
          <h3 class="mt-2 text-[clamp(1.45rem,2.2vw,1.9rem)] font-semibold text-[var(--text-primary)]">What happens next</h3>
          <p class="section-copy mt-2">{successText}</p>
        </div>
      </div>

      <ol class="mt-6 space-y-3">
        <li class="flex gap-4 rounded-[var(--radius-card)] border border-[color:var(--hairline)] bg-[color:var(--surface-overlay)] p-4">
          <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-[color:var(--hairline)] bg-[color:var(--surface-overlay-strong)] text-[var(--accent-solid)]" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
              <path d="M20 6L9 17L4 12" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"></path>
            </svg>
          </div>
          <div class="min-w-0">
            <p class="font-medium text-[var(--text-primary)]">We review your brief</p>
            <p class="mt-1 text-[16px] leading-[1.7] text-[var(--text-secondary)]">Checked and queued.</p>
          </div>
        </li>
        <li class="flex gap-4 rounded-[var(--radius-card)] border border-[color:var(--hairline)] bg-[color:var(--surface-overlay)] p-4">
          <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-[color:var(--hairline)] bg-[color:var(--surface-overlay-strong)] text-[var(--text-primary)]" aria-hidden="true">
            <span class="font-display text-[1rem] leading-none">2</span>
          </div>
          <div class="min-w-0">
            <p class="font-medium text-[var(--text-primary)]">You get a reply within 1 business day with the next step</p>
            <p class="mt-1 text-[16px] leading-[1.7] text-[var(--text-secondary)]">We&apos;ll review the scope, the site, and the fastest path forward.</p>
          </div>
        </li>
        <li class="flex gap-4 rounded-[var(--radius-card)] border border-[color:var(--hairline)] bg-[color:var(--surface-overlay)] p-4">
          <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-[color:var(--hairline)] bg-[color:var(--surface-overlay-strong)] text-[var(--text-primary)]" aria-hidden="true">
            <span class="font-display text-[1rem] leading-none">3</span>
          </div>
          <div class="min-w-0">
            <p class="font-medium text-[var(--text-primary)]">If there&apos;s a fit, we scope and quote - no commitment</p>
            <p class="mt-1 text-[16px] leading-[1.7] text-[var(--text-secondary)]">You&apos;ll know the next step before making any decision.</p>
          </div>
        </li>
      </ol>

      <div class="mt-6 flex flex-col gap-3 sm:flex-row sm:items-center">
        <Button asChild variant="secondary" size="md" className="w-full sm:w-auto">
          <a href="/work">While you wait, see real builds →</a>
        </Button>
        <Button
          type="button"
          variant="ghost"
          className="px-0 sm:w-auto"
          data-intake-reset
        >
          {kind === 'project' ? 'Send another brief' : 'Send another note'}
        </Button>
      </div>
    </div>

    <form
      data-axiom-intake-form
      data-intake-kind={kind}
      data-intake-endpoint={endpoint}
      class="flex flex-col gap-5 md:gap-6"
      aria-busy="false"
      noValidate
    >
      <div
        data-intake-alert
        class="hidden rounded-[var(--radius-card)] border border-[color:var(--error-border)] bg-[color:var(--error-surface)] px-4 py-3 text-[16px] text-[var(--error-text)] md:text-[15px]"
        role="alert"
        aria-live="assertive"
      ></div>

      <fieldset class="contents">
        <div class="grid grid-cols-1 gap-5 sm:grid-cols-2 md:gap-6">
          <div class="flex flex-col gap-1.5 md:gap-2" data-intake-field-group>
            <label for={`${kind}-name`} class={fieldLabelClass}>Name</label>
            <p id={`${kind}-name-helper`} class={fieldHelpClass}>Who should we address?</p>
            <input
              id={`${kind}-name`}
              name="name"
              type="text"
              required
              minLength="2"
              autoComplete="name"
              enterkeyhint="next"
              autocapitalize="words"
              spellcheck="false"
              class="data-intake-field"
              data-intake-field
              data-intake-field-key="name"
              data-intake-helper-id={`${kind}-name-helper`}
              data-intake-error-id={`${kind}-name-error`}
              data-intake-required-message="Please enter your name."
              data-intake-too-short-message="Please use at least 2 characters."
              aria-describedby={`${kind}-name-helper`}
            />
            <p id={`${kind}-name-error`} data-intake-field-error class={fieldErrorClass} hidden></p>
          </div>

          <div class="flex flex-col gap-1.5 md:gap-2" data-intake-field-group>
            <label for={`${kind}-email`} class={fieldLabelClass}>Email</label>
            <p id={`${kind}-email-helper`} class={fieldHelpClass}>Where should we reply?</p>
            <input
              id={`${kind}-email`}
              name="email"
              type="email"
              required
              autoComplete="email"
              enterkeyhint="next"
              autocapitalize="none"
              autocorrect="off"
              spellcheck="false"
              class="data-intake-field"
              data-intake-field
              data-intake-field-key="email"
              data-intake-helper-id={`${kind}-email-helper`}
              data-intake-error-id={`${kind}-email-error`}
              data-intake-required-message="Please enter an email address."
              data-intake-type-message="Please enter a valid email address."
              aria-describedby={`${kind}-email-helper`}
            />
            <p id={`${kind}-email-error`} data-intake-field-error class={fieldErrorClass} hidden></p>
          </div>

          <div class="flex flex-col gap-1.5 md:gap-2" data-intake-field-group>
            <label for={`${kind}-business`} class={fieldLabelClass}>Business name</label>
            <p id={`${kind}-business-helper`} class={fieldHelpClass}>Optional, if the reply should use a company name.</p>
            <input
              id={`${kind}-business`}
              name="business_name"
              type="text"
              autoComplete="organization"
              enterkeyhint="next"
              autocapitalize="words"
              spellcheck="false"
              class="data-intake-field"
              data-intake-field
              data-intake-field-key="business_name"
              data-intake-helper-id={`${kind}-business-helper`}
              data-intake-error-id={`${kind}-business-error`}
              aria-describedby={`${kind}-business-helper`}
            />
            <p id={`${kind}-business-error`} data-intake-field-error class={fieldErrorClass} hidden></p>
          </div>

          {kind === 'project' ? (
            <div class="flex flex-col gap-1.5 md:gap-2" data-intake-field-group>
              <label for={`${kind}-website`} class={fieldLabelClass}>Website URL</label>
              <p id={`${kind}-website-helper`} class={fieldHelpClass}>Optional. Add it if there is already a site.</p>
              <input
                id={`${kind}-website`}
                name="current_website"
                type="url"
                placeholder="https://example.com"
                autoComplete="url"
                enterkeyhint="next"
                autocapitalize="none"
                autocorrect="off"
                spellcheck="false"
                class="data-intake-field"
                data-intake-field
                data-intake-field-key="current_website"
                data-intake-helper-id={`${kind}-website-helper`}
                data-intake-error-id={`${kind}-website-error`}
                data-intake-type-message="Please enter a valid website URL."
                aria-describedby={`${kind}-website-helper`}
              />
              <p id={`${kind}-website-error`} data-intake-field-error class={fieldErrorClass} hidden></p>
            </div>
          ) : null}
        </div>

        {kind === 'project' ? (
          <div class="flex flex-col gap-2" data-intake-field-group>
            <label class={fieldLabelClass}>Preferred pricing path</label>
            <p class={fieldHelpClass}>Optional. Helps us answer with the right starting point.</p>
            <div class="grid gap-2 sm:grid-cols-3">
              {pricingPathOptions.map((option, index) => (
                <label class="group relative cursor-pointer">
                  <input
                    type="radio"
                    name="pricing_path"
                    value={option.value}
                    class="peer sr-only"
                    data-intake-field
                    data-intake-field-key="pricing_path"
                    checked={index === 2}
                  />
                  <span class="flex min-h-11 items-center justify-center rounded-[12px] border border-[color:var(--hairline)] bg-[color:var(--surface-overlay)] px-3 text-center text-[14px] font-medium leading-[1.45] text-[var(--text-secondary)] transition-[border-color,background-color,color,box-shadow] duration-200 ease-out peer-checked:border-[color:var(--accent-border)] peer-checked:bg-[color:var(--accent-surface)] peer-checked:text-[var(--text-primary)] peer-focus-visible:border-[color:var(--accent-border)] peer-focus-visible:ring-2 peer-focus-visible:ring-[color:var(--accent-ring)] peer-focus-visible:ring-offset-0">
                    {option.label}
                  </span>
                </label>
              ))}
            </div>
          </div>
        ) : null}

        <div class="flex flex-col gap-1.5 md:gap-2" data-intake-field-group>
          <label for={`${kind}-details`} class={fieldLabelClass}>
            {kind === 'project' ? 'Brief project description' : 'Message'}
          </label>
          <p id={`${kind}-details-helper`} class={fieldHelpClass}>{helper}</p>
          <textarea
            id={`${kind}-details`}
            name={kind === 'project' ? 'details' : 'message'}
            rows="5"
            required
            minLength="10"
            placeholder={kind === 'project' ? "What does the business do, what&apos;s broken on the current site, and when do you want to move?" : 'Tell us what you need help with.'}
            enterkeyhint="send"
            class="data-intake-field data-intake-textarea"
            data-intake-field
            data-intake-field-key={kind === 'project' ? 'details' : 'message'}
            data-intake-helper-id={`${kind}-details-helper`}
            data-intake-error-id={`${kind}-details-error`}
            data-intake-required-message={kind === 'project' ? 'Please share a little more detail.' : 'Please add a message.'}
            data-intake-too-short-message="Please share a little more detail."
            aria-describedby={`${kind}-details-helper`}
          ></textarea>
          <p id={`${kind}-details-error`} data-intake-field-error class={fieldErrorClass} hidden></p>
        </div>

        <div class="absolute left-[-10000px] top-auto h-px w-px overflow-hidden" aria-hidden="true">
          <label for={`${kind}-company-fax`}>Company Fax</label>
          <input
            id={`${kind}-company-fax`}
            type="text"
            name="company_fax"
            tabindex="-1"
            autocomplete="off"
            class="data-intake-field"
          />
        </div>
      </fieldset>

      <input type="hidden" name="source_path" value={Astro.url.pathname + Astro.url.search} />

      <div class="flex flex-col gap-4">
        <Button type="submit" size="lg" className="w-full rounded-[1rem]">
          {submitLabel}
        </Button>
        <p class="text-[14px] leading-[1.65] text-[var(--text-secondary)] md:text-[15px]">
          {noteText}{' '}
          <a href={noteLinkHref} class="underline decoration-[color:var(--border-strong)] underline-offset-2 transition-[color,text-decoration-color] duration-200 hover:text-[var(--accent-solid)] hover:decoration-current focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[color:var(--accent-ring)]">
            {noteLinkLabel}
          </a>
          .
        </p>
      </div>
    </form>
  </div>
</article>

<style>
  [data-axiom-intake-form] {
    --intake-field-border: var(--hairline);
    --intake-field-border-hover: var(--hairline-strong);
    --intake-field-border-focus: var(--accent-border);
    --intake-field-border-error: var(--text-error);
    --intake-field-bg: var(--surface-input);
    --intake-field-bg-hover: var(--surface-input-hover);
    --intake-field-bg-focus: var(--surface-input-focus);
  }

  [data-axiom-intake-form] [data-intake-field] {
    appearance: none;
    -webkit-appearance: none;
    width: 100%;
    min-height: 3.5rem;
    border-radius: 10px;
    border: 1px solid var(--intake-field-border);
    background: var(--intake-field-bg);
    padding: 0.875rem 1rem;
    color: var(--text-primary);
    font-size: 16px;
    line-height: 1.5;
    box-shadow: var(--shadow-field-base);
    caret-color: var(--text-primary);
  }

  [data-axiom-intake-form] [data-intake-field].data-intake-textarea {
    min-height: 10.5rem;
    resize: vertical;
    padding-top: 1rem;
    padding-bottom: 1rem;
  }

  [data-axiom-intake-form] [data-intake-field]::placeholder {
    color: var(--text-placeholder);
    opacity: 1;
  }

  [data-axiom-intake-form] [data-intake-field]:hover:not(:disabled) {
    border-color: var(--intake-field-border-hover);
    background: var(--intake-field-bg-hover);
  }

  [data-axiom-intake-form] [data-intake-field]:focus-visible {
    border-color: var(--intake-field-border-focus);
    background: var(--intake-field-bg-focus);
    box-shadow: var(--shadow-field-focus), var(--shadow-field-base);
    outline: none;
  }

  [data-axiom-intake-form] [data-intake-field][aria-invalid='true'] {
    border-color: var(--intake-field-border-error);
  }

  [data-axiom-intake-form] [data-intake-field][aria-invalid='true']:focus-visible {
    border-color: var(--intake-field-border-focus);
    box-shadow: var(--shadow-field-focus-error), var(--shadow-field-base);
  }

  [data-axiom-intake-form] [data-intake-field]:disabled {
    cursor: not-allowed;
    opacity: 0.72;
  }

  [data-axiom-intake-form] [data-intake-field-error]:not([hidden]) {
    display: block;
  }

  [data-axiom-intake-form] [data-intake-alert]:not([hidden]) {
    display: block;
  }

  [data-axiom-intake-form] [data-intake-success] {
    scroll-margin-top: 6rem;
  }

  [data-axiom-intake-form] input:-webkit-autofill,
  [data-axiom-intake-form] input:-webkit-autofill:hover,
  [data-axiom-intake-form] input:-webkit-autofill:focus,
  [data-axiom-intake-form] textarea:-webkit-autofill,
  [data-axiom-intake-form] textarea:-webkit-autofill:hover,
  [data-axiom-intake-form] textarea:-webkit-autofill:focus {
    -webkit-text-fill-color: var(--text-primary);
    caret-color: var(--text-primary);
    box-shadow: var(--shadow-field-autofill);
    transition: background-color 9999s ease-in-out 0s, color 9999s ease-in-out 0s;
  }
  
  [data-axiom-intake-form] button[type='submit']:focus-visible {
    box-shadow: var(--shadow-field-focus), var(--shadow-field-submit-focus);
  }
</style>

<script is:inline>
  const initIntakeForms = () => {
    const forms = document.querySelectorAll('[data-axiom-intake-form]');
    const FIELD_CONFIG = {
      name: {
        required: 'Please enter your name.',
        tooShort: 'Please use at least 2 characters.'
      },
      email: {
        required: 'Please enter an email address.',
        typeMismatch: 'Please enter a valid email address.'
      },
      business_name: {},
      current_website: {
        typeMismatch: 'Please enter a valid website URL.'
      },
      details: {
        required: 'Please share a little more detail.',
        tooShort: 'Please share a little more detail.'
      },
      message: {
        required: 'Please add a message.',
        tooShort: 'Please share a little more detail.'
      }
    };

    const updateFieldState = (form, field, force = false) => {
      if (!(field instanceof HTMLElement)) return true;

      const key = field.dataset.intakeFieldKey || '';
      const helperId = field.dataset.intakeHelperId || '';
      const errorId = field.dataset.intakeErrorId || '';
      const errorEl = errorId ? form.querySelector(`#${errorId}`) : null;
      const isTouched = force || field.dataset.touched === 'true' || form.dataset.validationAttempted === 'true';
      let error = '';

      if (isTouched && 'validity' in field) {
        const config = FIELD_CONFIG[key] || {};
        if (field.validity.valueMissing) {
          error = config.required || 'Please complete this field.';
        } else if (field.validity.typeMismatch) {
          error = config.typeMismatch || 'Please enter a valid value.';
        } else if (field.validity.tooShort) {
          error = config.tooShort || 'Please use a longer value.';
        }
      }

      if (errorEl instanceof HTMLElement) {
        errorEl.textContent = error;
        errorEl.hidden = !error;
      }

      if (field instanceof HTMLInputElement || field instanceof HTMLTextAreaElement) {
        if (error) {
          field.setAttribute('aria-invalid', 'true');
          field.dataset.fieldState = 'invalid';
        } else {
          field.removeAttribute('aria-invalid');
          delete field.dataset.fieldState;
        }

        if (helperId) {
          field.setAttribute('aria-describedby', error ? `${helperId} ${errorId}` : helperId);
        }
      }

      return error.length === 0;
    };

    forms.forEach((form) => {
      if (!(form instanceof HTMLFormElement)) return;
      if (form.dataset.intakeInitialized === 'true') return;
      form.dataset.intakeInitialized = 'true';
      form.dataset.validationAttempted = 'false';

      const endpoint = form.dataset.intakeEndpoint || '/api/contact';
      const alertBox = form.querySelector('[data-intake-alert]');
      const successBox = form.closest('article')?.querySelector('[data-intake-success]');
      const resetButton = form.closest('article')?.querySelector('[data-intake-reset]');
      const submitButton = form.querySelector('button[type="submit"]');
      const fields = Array.from(form.querySelectorAll('[data-intake-field]'));

      const showAlert = (message) => {
        if (!(alertBox instanceof HTMLElement)) return;
        alertBox.textContent = message;
        alertBox.classList.remove('hidden');
      };

      const clearAlert = () => {
        if (!(alertBox instanceof HTMLElement)) return;
        alertBox.textContent = '';
        alertBox.classList.add('hidden');
      };

      const syncAllFieldStates = (force = false) => {
        let firstInvalidField = null;
        let hasErrors = false;

        fields.forEach((field) => {
          const isValid = updateFieldState(form, field, force);
          if (!isValid && !firstInvalidField) {
            firstInvalidField = field;
          }
          if (!isValid) {
            hasErrors = true;
          }
        });

        return { firstInvalidField, hasErrors };
      };

      const setBusy = (busy) => {
        form.setAttribute('aria-busy', String(busy));
        form.dataset.submitting = String(busy);
        fields.forEach((field) => {
          if (field instanceof HTMLInputElement || field instanceof HTMLTextAreaElement) {
            field.disabled = busy;
          }
        });
        if (submitButton instanceof HTMLButtonElement) {
          submitButton.disabled = busy;
          submitButton.textContent = busy ? 'Sending...' : form.dataset.intakeKind === 'project' ? 'Submit project brief →' : 'Send note →';
        }
      };

      const resetToForm = () => {
        form.reset();
        clearAlert();
        form.dataset.validationAttempted = 'false';
        delete form.dataset.submitting;
        fields.forEach((field) => {
          if (field instanceof HTMLInputElement || field instanceof HTMLTextAreaElement) {
            field.disabled = false;
            field.dataset.touched = 'false';
            field.removeAttribute('aria-invalid');
            delete field.dataset.fieldState;
            const errorId = field.dataset.intakeErrorId || '';
            const errorEl = errorId ? form.querySelector(`#${errorId}`) : null;
            if (errorEl instanceof HTMLElement) {
              errorEl.textContent = '';
              errorEl.hidden = true;
            }
          }
        });
        if (successBox instanceof HTMLElement) {
          successBox.classList.add('hidden');
        }
        form.classList.remove('hidden');
        if (submitButton instanceof HTMLButtonElement) {
          submitButton.disabled = false;
        }
      };

      if (resetButton instanceof HTMLButtonElement) {
        resetButton.addEventListener('click', resetToForm);
      }

      fields.forEach((field) => {
        if (!(field instanceof HTMLInputElement || field instanceof HTMLTextAreaElement)) return;

        field.addEventListener('blur', () => {
          field.dataset.touched = 'true';
          updateFieldState(form, field);
        });

        field.addEventListener('input', () => {
          if (field.dataset.touched === 'true' || form.dataset.validationAttempted === 'true') {
            updateFieldState(form, field);
          }
        });
      });

      form.addEventListener('submit', async (event) => {
        event.preventDefault();
        form.dataset.validationAttempted = 'true';
        clearAlert();
        const { firstInvalidField, hasErrors } = syncAllFieldStates(true);

        if (hasErrors) {
          showAlert('Please review the highlighted fields.');
          if (firstInvalidField instanceof HTMLElement) {
            window.requestAnimationFrame(() => {
              firstInvalidField.focus({ preventScroll: false });
            });
          }
          return;
        }

        const formData = new FormData(form);
        const payload = Object.fromEntries(formData.entries());

        setBusy(true);

        const body = form.dataset.intakeKind === 'project'
          ? {
              name: String(payload.name || '').trim(),
              email: String(payload.email || '').trim(),
              business_name: String(payload.business_name || '').trim(),
              current_website: String(payload.current_website || '').trim(),
              pricing_path: String(payload.pricing_path || '').trim(),
              details: String(payload.details || '').trim(),
              primary_goal: 'new_site',
              source_path: String(payload.source_path || '').trim(),
              company_fax: String(payload.company_fax || '').trim(),
            }
          : {
              name: String(payload.name || '').trim(),
              email: String(payload.email || '').trim(),
              business_name: String(payload.business_name || '').trim(),
              message: String(payload.message || '').trim(),
              source_path: String(payload.source_path || '').trim(),
              company_fax: String(payload.company_fax || '').trim(),
            };

        try {
          const response = await fetch(endpoint, {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify(body)
          });

          const result = await response.json().catch(() => null);
          if (!response.ok || !result?.ok) {
            const fallbackError = form.dataset.intakeKind === 'project'
              ? 'We could not send your project brief. Please try again or email contact@getaxiom.ca.'
              : 'We could not send your note. Please try again or email contact@getaxiom.ca.';
            throw new Error(result?.details || result?.error || fallbackError);
          }

          if (successBox instanceof HTMLElement) {
            form.classList.add('hidden');
            successBox.classList.remove('hidden');
            successBox.scrollIntoView({ behavior: 'smooth', block: 'start' });
          }

          form.reset();
        } catch (error) {
          const message = error instanceof Error && error.message ? error.message : 'Something went wrong.';
          showAlert(message);
        } finally {
          setBusy(false);
        }
      });
    });
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initIntakeForms, { once: true });
  } else {
    initIntakeForms();
  }
</script>
