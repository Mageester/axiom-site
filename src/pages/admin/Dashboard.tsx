import DashboardClient from "@omni/pages/DashboardClient";
import OmniRoot from "@omni/OmniRoot";

export default function Dashboard() {
  return (
    <OmniRoot>
      <DashboardClient />
    </OmniRoot>
  );
}
