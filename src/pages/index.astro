---
import ResponsivePicture from '../components/site/ResponsivePicture.astro';
import SiteLayout from '../layouts/SiteLayout.astro';
import { CTA_PATHS } from '../lib/cta';
import { HOME_JSON_LD } from '../lib/seo';
import { getWorkProofImage } from '../lib/workProofImages';
import { FadeIn } from '../components/ui/FadeIn';
import { Button } from '../components/ui/button';
import { Card } from '../components/ui/card';
import { Eyebrow } from '../components/ui/Eyebrow';
import DrawIcon from '../components/ui/DrawIcon.astro';

const browserBars = [
  { width: '92%', delay: 0 },
  { width: '78%', delay: 0.4 },
  { width: '64%', delay: 0.8 },
] as const;

const workCards = [
  {
    eyebrow: 'SALON & BEAUTY',
    title: 'Nails Studio Demo',
    outcome: 'Clean booking flow paired with modern trust cues and a polished luxury feel.',
    metricLabel: 'LOAD TARGET',
    metricValue: '<1s',
    image: getWorkProofImage('concept-salon-demo').source,
    alt: getWorkProofImage('concept-salon-demo').alt || 'Nails Studio Demo',
    position: getWorkProofImage('concept-salon-demo').position || 'center 40%',
  },
  {
    eyebrow: 'PERSONAL SERVICES & GROOMING',
    title: 'Blackline Barber Co.',
    outcome: 'Sharp editorial branding leading to a frictionless, high-speed booking flow.',
    metricLabel: 'BOOKING PATH',
    metricValue: '1 TAP',
    image: getWorkProofImage('concept-barber-demo').source,
    alt: getWorkProofImage('concept-barber-demo').alt || 'Blackline Barber Co.',
    position: getWorkProofImage('concept-barber-demo').position || 'center 40%',
  },
  {
    eyebrow: 'HEALTHCARE & MEDICAL',
    title: 'Aurelia Dental Studio',
    outcome: 'Calm, trust-focused presentation paired with a polished booking experience.',
    metricLabel: 'MOBILE TARGET',
    metricValue: '98+',
    image: getWorkProofImage('concept-dental-demo').source,
    alt: getWorkProofImage('concept-dental-demo').alt || 'Aurelia Dental Studio',
    position: getWorkProofImage('concept-dental-demo').position || 'center 40%',
  },
] as const;

const principles = [
  {
    number: '01',
    title: 'No unclear offer.',
    description: "If the business isn't readable in three seconds, the build isn't ready.",
    icon: 'eye',
  },
  {
    number: '02',
    title: 'No buried proof.',
    description: 'Real work, real reviews, and real results belong where buyers actually look.',
    icon: 'shield-check',
  },
  {
    number: '03',
    title: 'No slow first load.',
    description: 'Sub-second loads are the floor. If it drags, we fix it.',
    icon: 'bolt',
  },
  {
    number: '04',
    title: 'No dead-end handoff.',
    description: 'Monthly support stays available. One-time ownership stays clean.',
    icon: 'key',
  },
] as const;

const processStages = [
  {
    day: 'Day 1',
    title: 'Review',
    description: 'We audit the current site, the offer, and the gaps that are costing trust.',
    icon: 'search',
  },
  {
    day: 'Day 3',
    title: 'Scope',
    description: 'We lock the structure, the message, and the pages that actually matter.',
    icon: 'document',
  },
  {
    day: 'Day 7',
    title: 'Build',
    description: 'The design system, layout, and content come together in production.',
    icon: 'code',
  },
  {
    day: 'Day 14',
    title: 'Launch',
    description: 'We ship the site, verify the handoff, and make ownership complete.',
    icon: 'arrow-up',
  },
] as const;

const services = [
  {
    label: 'Monthly growth site',
    description:
      'Best for businesses that want lower upfront cost, ongoing support, and a site that keeps improving after launch.',
    href: '/work',
    icon: 'target',
  },
  {
    label: 'Ownership build',
    description:
      'Best for teams that prefer to buy outright and own the code from day one.',
    href: '/work',
    icon: 'crown',
  },
  {
    label: 'Rebuild & Migrate',
    description:
      'Best for replacing an underperforming site without downtime or a messy handoff.',
    href: '/process',
    icon: 'refresh',
  },
] as const;

---

<SiteLayout
  title="Custom Web Development | Kitchener-Waterloo | Axiom Infrastructure"
  description="Axiom Infrastructure designs custom websites and AI-enabled digital systems for established businesses across Kitchener-Waterloo, Cambridge, and Guelph."
  canonicalPath="/"
  ogImageAlt="Axiom Infrastructure homepage - custom web development in Kitchener-Waterloo."
  schema={HOME_JSON_LD}
>
  <main id="main-content" tabindex="-1" class="-mt-28 md:-mt-32">
    <section class="relative overflow-hidden pb-20 pt-32 md:pb-28 md:pt-36">
      <div
        aria-hidden="true"
        class="pointer-events-none absolute inset-0"
        style="
          background:
            radial-gradient(circle at 82% 12%, var(--accent-surface) 0%, var(--accent-surface-strong) 14%, transparent 36%),
            linear-gradient(180deg, var(--surface-panel-strong) 0%, transparent 18%);
        "
      />
      <div
        aria-hidden="true"
        class="pointer-events-none absolute inset-0 opacity-[0.03]"
        style="
          background-image:
            radial-gradient(circle at 20% 20%, var(--hero-noise-dot) 0.6px, transparent 0.6px),
            radial-gradient(circle at 80% 70%, var(--hero-noise-dot-alt) 0.6px, transparent 0.6px);
          background-size: 4px 4px, 5px 5px;
          background-position: 0 0, 1px 1px;
          mix-blend-mode: overlay;
        "
      />
      <div class="axiom-container relative">
        <div class="grid gap-10 lg:grid-cols-[minmax(0,1fr)_minmax(20rem,27rem)] lg:items-center">
          <FadeIn client:load className="max-w-3xl">
            <Eyebrow className="text-[12px] text-[var(--text-secondary)]">WEB DESIGN AGENCY</Eyebrow>
            <h1 class="mt-6 max-w-4xl text-left">
              Websites that turn visitors into booked calls.
            </h1>
            <p class="section-copy mt-6 max-w-2xl">
              Axiom builds fast, clear websites for established local businesses that need more calls and less friction.
              Most clients start monthly for lower upfront cost and ongoing support; a one-time ownership path is
              available when it fits better.
            </p>

            <div class="mt-8 flex flex-col gap-3 sm:flex-row sm:items-center">
              <Button asChild size="lg" className="w-full sm:w-auto">
                <a href={CTA_PATHS.startProject}>Start a project</a>
              </Button>
              <Button asChild variant="ghost" size="lg" className="w-full px-0 sm:w-auto">
                <a href="/work">See the work</a>
              </Button>
            </div>

            <p class="mt-3 text-[13px] uppercase tracking-[0.08em] text-[var(--text-muted)]">
              Monthly plans include ongoing support. One-time ownership is available when you want it.
            </p>
          </FadeIn>

          <div class="hidden justify-self-end lg:block" aria-hidden="true">
            <div class="hero-browser-shell">
              <svg viewBox="0 0 520 340" class="block h-auto w-full" fill="none" role="presentation">
                <rect x="0.75" y="0.75" width="518.5" height="338.5" rx="24" class="hero-browser-frame" />
                <rect x="0.75" y="0.75" width="518.5" height="52" rx="24" class="hero-browser-chrome" />
                <circle cx="36" cy="26" r="5.5" class="hero-browser-dot" />
                <circle cx="54" cy="26" r="5.5" class="hero-browser-dot" />
                <circle cx="72" cy="26" r="5.5" class="hero-browser-dot" />

                {browserBars.map((bar, index) => (
                  <rect
                    key={index}
                    x="42"
                    y={86 + index * 46}
                    width={bar.width}
                    height="16"
                    rx="8"
                    class="hero-browser-bar"
                    style={`--browser-delay: ${bar.delay}s;`}
                  />
                ))}
              </svg>
            </div>
          </div>

          <div class="flex justify-center lg:col-span-2">
            <p class="text-center font-axiomDisplay text-[clamp(1.2rem,2vw,1.85rem)] leading-[1.08] tracking-[-0.04em] text-[var(--text-primary)]">
              Fourteen days. <span class="text-[var(--accent-solid)]">Under one second.</span> Ongoing support included.
            </p>
          </div>
        </div>
      </div>
    </section>

    <section class="page-section">
      <div class="axiom-container">
        <FadeIn client:load className="max-w-3xl">
          <Eyebrow>SELECTED WORK</Eyebrow>
          <h2 class="section-heading mt-4">
            Built for businesses that measure results.
          </h2>
        </FadeIn>

        <div class="mt-10 grid gap-6 md:grid-cols-2 xl:grid-cols-3">
          {workCards.map((card, index) => (
            <FadeIn client:load delay={Math.min(index * 100, 400)}>
              <Card
                className="group overflow-hidden p-0 transition-[transform,background-color,border-color,box-shadow] duration-200 ease-[cubic-bezier(0.4,0,0.2,1)] hover:-translate-y-1 hover:bg-[var(--surface-2)] hover:border-[color:var(--hairline-strong)]"
              >
                <figure class="relative overflow-hidden border-b border-[color:var(--hairline)] bg-white/5">
                  <ResponsivePicture
                    source={card.image}
                    alt={card.alt}
                    sizes="(min-width: 1280px) 32vw, (min-width: 768px) 45vw, 100vw"
                    class="aspect-[4/3] h-full w-full object-cover transition-transform duration-500 ease-[cubic-bezier(0.4,0,0.2,1)] group-hover:scale-[1.03]"
                    loading="lazy"
                    fetchpriority="low"
                    objectPosition={card.position}
                  />
                  <div class="pointer-events-none absolute inset-0 bg-[linear-gradient(to_top,var(--overlay-black-soft),transparent)]" />
                </figure>

                <div class="flex h-full flex-col p-6 sm:p-7">
                  <Eyebrow>{card.eyebrow}</Eyebrow>
                  <h3 class="mt-4 text-[22px] font-semibold tracking-[-0.01em] text-[var(--text-primary)]">
                    {card.title}
                  </h3>
                  <p class="section-copy mt-3">
                    {card.outcome}
                  </p>

                  <div class="mt-auto flex items-center justify-between gap-4 border-t border-[color:var(--hairline)] pt-5">
                    <span class="axiom-metric text-[var(--text-muted)]">{card.metricLabel}</span>
                    <span class="text-[15px] font-semibold tracking-[0.08em] text-[var(--text-primary)]">
                      {card.metricValue}
                    </span>
                  </div>
                </div>
              </Card>
            </FadeIn>
          ))}
        </div>
      </div>
    </section>

    <section class="page-section">
      <div class="axiom-container">
        <FadeIn client:load className="max-w-3xl">
          <Eyebrow>WHAT WE BUILD</Eyebrow>
          <h2 class="section-heading mt-4">Three formats. One outcome.</h2>
        </FadeIn>

        <div class="mt-10 grid gap-5 md:grid-cols-3">
          {services.map((service, index) => (
            <FadeIn client:load delay={Math.min(index * 100, 400)} as="div">
              <a
                href={service.href}
                class="group flex h-full flex-col gap-4 rounded-[var(--radius-card)] border border-[color:var(--hairline)] bg-[color:var(--surface-1)] p-6 transition-[border-color,background-color] duration-200 hover:border-[color:var(--hairline-strong)] hover:bg-[var(--surface-2)] sm:p-7"
              >
                <DrawIcon name={service.icon} />
                  <p class="text-[20px] font-semibold tracking-[-0.01em] text-[var(--text-primary)]">
                    {service.label}
                  </p>
                <p class="section-copy">
                  {service.description}
                </p>
                <span class="mt-auto font-mono text-[12px] uppercase tracking-[0.12em] text-[var(--accent-solid)] transition-opacity duration-200 group-hover:opacity-80">
                  See examples
                </span>
              </a>
            </FadeIn>
          ))}
        </div>
      </div>
    </section>

    <section class="page-section">
      <div class="axiom-container">
        <FadeIn client:load className="max-w-3xl">
          <Eyebrow>WHY AXIOM</Eyebrow>
          <h2 class="section-heading mt-4">What we won't ship.</h2>
        </FadeIn>

        <div class="mt-10 grid gap-5 md:grid-cols-2">
          {principles.map((principle, index) => (
            <FadeIn client:load delay={Math.min(index * 100, 400)} as="div">
              <Card className="h-full p-0">
                <div class="flex h-full flex-col p-6 sm:p-7">
                  <DrawIcon name={principle.icon} className="mb-4" />
                  <Eyebrow className="text-[var(--text-muted)]">{principle.number}</Eyebrow>
                  <h3 class="mt-4 text-[22px] font-semibold tracking-[-0.01em] text-[var(--text-primary)]">
                    {principle.title}
                  </h3>
                  <p class="section-copy mt-3">
                    {principle.description}
                  </p>
                </div>
              </Card>
            </FadeIn>
          ))}
        </div>
      </div>
    </section>

    <section class="page-section">
      <div class="axiom-container">
        <FadeIn client:load className="max-w-3xl">
          <Eyebrow>HOW WE BUILD</Eyebrow>
          <h2 class="section-heading mt-4">
            Four stages. Fourteen days. No drift.
          </h2>
        </FadeIn>

        <div class="mt-10 flex flex-col gap-4 lg:flex-row lg:flex-nowrap">
          {processStages.map((stage, index) => (
            <FadeIn client:load delay={Math.min(index * 100, 400)} as="div">
              <Card className="flex-1 min-w-[220px] p-0">
                <div class="flex h-full flex-col gap-4 p-6 sm:p-7">
                  <div class="flex items-center gap-2.5">
                    <DrawIcon name={stage.icon} size={15} className="text-[var(--accent-solid)]" />
                    <span class="font-mono text-[12px] uppercase tracking-[0.08em] text-[var(--accent-solid)]">
                      {stage.day}
                    </span>
                  </div>
                  <div>
                    <p class="text-[18px] font-semibold tracking-[-0.01em] text-[var(--text-primary)]">
                      {stage.title}
                    </p>
                    <p class="section-copy mt-2">
                      {stage.description}
                    </p>
                  </div>
                </div>
              </Card>
            </FadeIn>
          ))}
        </div>

        <div class="mt-6">
          <Button asChild variant="ghost" className="px-0">
            <a href="/process">See the full process</a>
          </Button>
        </div>
      </div>
    </section>

    <section class="page-section">
      <div class="axiom-container">
        <FadeIn client:load as="div">
          <Card className="mx-auto max-w-5xl p-0 text-center">
            <div class="px-6 py-8 sm:px-8 sm:py-10 md:px-10 md:py-12">
              <Eyebrow className="justify-center">THE GUARANTEE</Eyebrow>
              <h2 class="section-heading mx-auto mt-4">
                If your site loads in over one second, we fix it free.
              </h2>
          <p class="section-copy mx-auto mt-4">
                No asterisks. Monthly-first support stays with you after launch.
              </p>
            </div>
          </Card>
        </FadeIn>
      </div>
    </section>

    <section class="border-t border-[color:var(--hairline)] bg-[var(--surface-2)]">
      <div class="axiom-container py-12 md:py-16">
        <div class="grid gap-8 lg:grid-cols-[minmax(0,1.2fr)_minmax(18rem,0.75fr)] lg:items-end">
          <FadeIn client:load as="div" className="max-w-3xl">
            <h2 class="section-heading text-[clamp(32px,4vw,52px)] font-[600] text-[var(--text-primary)]">
              If the site isn't earning trust, it's costing you work.
            </h2>
            <p class="section-copy mt-4">
                Start monthly for lower upfront cost and ongoing support. If buying outright makes more sense, we&apos;ll
                scope that too.
            </p>
          </FadeIn>

          <FadeIn client:load delay={100} as="div">
            <div class="flex flex-col gap-4 lg:items-end">
              <Button asChild size="lg" className="w-full sm:w-auto">
                <a href={CTA_PATHS.startProject}>Start a project</a>
              </Button>
              <p class="axiom-metric text-[var(--text-muted)]">
                Lower upfront cost &rarr; Ongoing support &rarr; Ownership optional
              </p>
            </div>
          </FadeIn>
        </div>
      </div>
    </section>
  </main>

  <style>
    .hero-browser-shell {
      width: clamp(18rem, 28vw, 27rem);
      opacity: 0.4;
      transform: translate3d(1.75rem, -0.5rem, 0) rotate(2deg);
      transform-origin: center;
      pointer-events: none;
      will-change: transform, opacity;
    }

    .hero-browser-frame {
      fill: rgba(255, 255, 255, 0.02);
      stroke: rgba(255, 255, 255, 0.1);
      stroke-width: 1;
    }

    .hero-browser-chrome {
      fill: rgba(255, 255, 255, 0.035);
    }

    .hero-browser-dot {
      fill: rgba(255, 255, 255, 0.22);
    }

    .hero-browser-bar {
      fill: var(--accent-solid);
      opacity: 0;
      transform-box: fill-box;
      transform-origin: left center;
      animation:
        browser-bar-reveal 1.2s cubic-bezier(0.4, 0, 0.2, 1) var(--browser-delay, 0s) forwards,
        browser-bar-pulse 8s ease-in-out calc(1.2s + var(--browser-delay, 0s)) infinite;
      will-change: opacity, transform;
    }

    @keyframes browser-bar-reveal {
      0% {
        opacity: 0;
        transform: translateY(5px);
      }

      100% {
        opacity: 0.6;
        transform: translateY(0);
      }
    }

    @keyframes browser-bar-pulse {
      0%,
      18%,
      100% {
        opacity: 0.6;
        transform: translateY(0);
      }

      48% {
        opacity: 0.42;
        transform: translateY(0);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .hero-browser-shell {
        opacity: 0.45;
        transform: translate3d(1.75rem, -0.5rem, 0) rotate(2deg);
      }

      .hero-browser-bar {
        animation: none !important;
        opacity: 0.55;
        transform: none;
      }
    }
  </style>
</SiteLayout>

